import {Container} from "@mui/material";

function Main() {
    return (
        <Container
            fixed
            sx={{
                background: "gray"
            }}
        >
            content
        </Container>
    );
}

export default Main;